name: Platform Build (arcd executor)

# NEUTRALIZED — the native pipeline is .hanzo/workflows/deploy.yml; GitHub is a mirror.
# Image build + operator rollout now run in-cluster (act_runner + BuildKit) on
# Hanzo Git push, patching the operator App CRs (cal, hanzo-app-base) directly.
# This GitHub Actions workflow is retained only as a sync notice.

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: hanzo-build-linux-amd64
    steps:
      - name: Sync notice
        run: echo "native pipeline is .hanzo/workflows/deploy.yml; GitHub is a mirror"
