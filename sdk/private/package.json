{
  "name": "@hanzo/private",
  "version": "0.1.0",
  "description": "Per-user encrypted blob store — Base SQLite, EVM contract, or IPFS. Same API.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "peerDependencies": {
    "viem": "^2.0.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^1.0.0"
  }
}
