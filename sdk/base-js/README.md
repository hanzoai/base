# Moved

The Hanzo Base JavaScript/TypeScript client (`@hanzo/base`) now lives in its
own repository:

**https://github.com/hanzo-js/base**

```sh
npm install @hanzo/base
```

The client SDKs no longer ship from this server repo. The Dart client
(`hanzo`) is at https://github.com/hanzo-dart/base.
