package core

// CollectionNameFunctions is where a Base keeps the functions it runs. One row
// is one function: its id is its name and its source is its body.
const CollectionNameFunctions = "_functions"
